Fix `muse.transforms.reshape_slit_step_to_x` to use recorded step spacing without converting the default angular pixel size, allowing round trips with physical distance coordinates such as Mm and cm.
